import React from 'react';
import StyledCounter from './styles/StyledCounter';

const FlagCounter = ({ flagsRemaining }) => <StyledCounter>{flagsRemaining}</StyledCounter>;

export default FlagCounter;
